#cloud-config
rancher:
  services_include:
    ubuntu-console: true

  services:
    rancher-agent-boot:
      image: ${rancher_agent_image}
      command: ${agent_registration_url}
      detatch: true
      privileged: true
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
